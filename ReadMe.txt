
主体结构使用MVC架构。
classes、resource文件夹里的东西可以根据实际情况，不用的就删除掉。
Utility、IQDB.xcdatamodeld请不要删除掉。

1、IQDB.xcdatamodeld，core data的封装实现，现在里边有一张表RecommendCertification，可以查找怎么使用。
2、classes，所有的类都在这个文件夹里。
3、resource，资源文件，图片都在里边，存放图片的时候按照功能模块存放在不同的文件夹下边。
4、Utility，常用工具类，有需要可以现在这里查找，一般的实现70%都有。